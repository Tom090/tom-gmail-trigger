import * as TE from 'fp-ts/TaskEither';
import { File, FileStorage, SharedUrl } from './File';
export declare class FakeFileStorage implements FileStorage {
    files: Map<string, File>;
    constructor();
    write(file: File): TE.TaskEither<Error, File>;
    read(fileName: string): TE.TaskEither<Error, File>;
    delete(fileName: string): TE.TaskEither<Error, undefined>;
    getSharedUrl(key: string): TE.TaskEither<Error, SharedUrl>;
    createDirectory(key: string): TE.TaskEither<Error, undefined>;
}
//# sourceMappingURL=FakeFileStorage.d.ts.map