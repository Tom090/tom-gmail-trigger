import * as TE from 'fp-ts/lib/TaskEither';
import { File, FileStorage, SharedUrl } from './File';
export declare class NodeFsFileStorage implements FileStorage {
    private basePath;
    constructor(basePath?: string);
    write(file: File): TE.TaskEither<Error, File>;
    read(key: string, name?: string): TE.TaskEither<Error, File>;
    getSharedUrl(key: string): TE.TaskEither<Error, SharedUrl>;
    delete(key: string): TE.TaskEither<Error, undefined>;
    createDirectory(key: string): TE.TaskEither<Error, undefined>;
}
//# sourceMappingURL=NodeFsFileStorage.d.ts.map